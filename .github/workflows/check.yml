name: Check
on: push
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: 語法檢查
        run: node --check data.js
      - name: 結構檢查
        run: node check-data.js